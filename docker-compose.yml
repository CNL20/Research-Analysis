services:
  postgres:
    image: postgres:16-alpine
    container_name: scholartrend-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ScholarTrendDb
    ports:
      - "5432:5432"
    volumes:
      - scholartrend_pgdata:/var/lib/postgresql/data

volumes:
  scholartrend_pgdata:
